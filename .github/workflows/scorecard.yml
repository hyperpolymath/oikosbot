# SPDX-License-Identifier: MPL-2.0
# This workflow is managed by gh actions-lock.
name: OSSF Scorecard

on:
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch:

permissions:
  actions: read
  contents: read

jobs:
  scorecard:
    permissions:
      contents: read
      # Publishes Scorecard SARIF findings to this repository's code scanning UI.
      security-events: write
      # Lets the reusable workflow request an OIDC token for signed provenance.
      id-token: write
    uses: hyperpolymath/standards/.github/workflows/scorecard-reusable.yml@da2c748aad55c1a1dcba00b60fe4a35017bc6540
