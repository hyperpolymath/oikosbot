// SPDX-License-Identifier: CC-BY-SA-4.0
// SPDX-FileCopyrightText: 2026 Jonathan D.A. Jewell <j.d.a.jewell@open.ac.uk>
= OikosBot Roadmap
Jonathan D.A. Jewell <j.d.a.jewell@open.ac.uk>
v0.1.0-dev, 2026-06-19
:toc: left
:toclevels: 2
:icons: font

This roadmap tracks OikosBot, the ecological and economic code-analysis app.
The Oikos economics accounting DSL now lives in
https://github.com/hyperpolymath/oikos-economics-accounting-dsl[`hyperpolymath/oikos-economics-accounting-dsl`].

Status legend: `done` | `in-progress` | `blocked` | `planned`

== v0.1 - Scaffold

*Status:* in-progress

=== Done

* [x] Haskell analyzer scaffold at `analyzers/code-haskell/`
* [x] AffineScript webhook receiver scaffold at `bot-integration-affine/`
* [x] GitHub App manifest at `.github/app.yml`
* [x] Policy rules in Datalog and DeepProbLog
* [x] Bot configuration, prompt templates, and database schema assets
* [x] Repository split from the Oikos DSL

=== Done (2026-07-28)

* [x] Pareto engine in the shipping path (`crates/oikosbot-pareto`): ε-tolerant
  dominance, frontier, normalized weighted ParetoScore, base-vs-head PR
  verdicts (improvement/regression/trade-off/neutral) with confidence gating,
  `oikosbot compare`, SARIF `pareto_*` properties, EconScore composition per
  ARCHITECTURE.adoc
* [x] `.oikos.yml` config support in the CLI (`--config` + auto-discovery):
  mode, thresholds, exclude globs, languages — makes the estate consumer
  configs and the governance workflow's `--config` flag real
* [x] Composite GitHub Action (`action.yml`, container-based) — OikosBot
  consumable as `uses: hyperpolymath/oikosbot@<ref>`; Marketplace-shaped
* [x] Marketplace positioning doc: `docs/COMPARISON-climate-warrior.adoc`
* [x] publish-image root-caused: GHCR `oikos` package lacked Actions write
  access for this repo (`permission_denied: write_package`); fixed by owner
  grant, not by code — the Containerfile was verified sound via local podman

=== Done (2026-08-03) — estate economics, round one

* [x] *New unit of analysis: the estate.* Crates `oikosbot-telemetry`
  (gh-API collector, Parquet snapshots, derived energy/carbon/cost with the
  confidence ladder), `oikosbot-capability` (verified-capability output metrics
  including fake-gate detection from run history), `oikosbot-dea` (Data
  Envelopment Analysis — input-oriented CCR and BCC as paired
  envelopment/multiplier LPs on `good_lp`+HiGHS, returning θ, peer reference
  sets and dual weights). PR #60.
* [x] `oikosbot estate collect|analyse|report` — resumable collection,
  deterministic analysis, markdown/JSON reporting.
* [x] Snapshot dataset repository
  https://github.com/hyperpolymath/oikosbot-estate[`oikosbot-estate`].
* [x] *First `Measured` data in the project's history* — wall-clock minutes
  from the GitHub API.
* [x] Premise falsified before building on it: `pearson(wall_minutes,
  size_kb) = -0.049` across 381 repositories.
* [x] CI unblocked — Actions workflow lockfile adopted (#61), reusable-caller
  permissions fixed (#63).
* [x] Debt register link:DEBT.adoc[`DEBT.adoc`] and a documentation pass
  across README, STATUS, ARCHITECTURE, the a2ml set and the wiki.

=== Remaining

*Round two is decided by evidence, not pre-committed.* The candidates are
(A) export the DEA duals as shadow prices and wire Eclexia's currently-inert
`@optimize` through a shared dominance crate; (B) enforcement — wire
`calibration.rs` so `--check` can finally block (#48); (C) deepen the
substrate — stock-flow-consistency identity checks, static capability signals,
longer backfill. The decision criterion is reported by the pipeline itself:
the independence matrix, dual stability across at least two snapshots, and
telemetry coverage. **Requires snapshot #2.**

* [ ] *Consumer lockfile sweep (P1)* — 15 estate repositories carry
  `oikosbot.yml` merged and pinned, but each needs its own Actions lockfile
  before the workflow can start. OikosBot is installed everywhere and running
  nowhere; this is the highest-value follow-up.
* [ ] *PR decoration (P2)* — a single updated-in-place comment with a verdict
  header and honest estimated/measured counts. The one table stake every
  comparable product has and OikosBot lacks; SARIF-to-code-scanning is not a
  substitute, because it does not gate merges and reviewers do not open the
  Security tab during review. Blocked on nothing.
* [ ] Stabilize the analyzer request and response schema
* [ ] Wire the AffineScript receiver to the analyzer service
* [ ] Replace mock health reports with analyzer-backed reports
* [ ] Add a repeatable local development runtime
* [ ] Decide how to package the bot service and policy engine
* [ ] Port `policy-engine/datalog/eco_rules.dl` to an executable Scallop
  (Rust) engine; retire the Python DeepProbLog assets (ruled 2026-07-28 —
  no interim Python)

== Data Layer — Single Store (VeriSimDB)

*Status:* in-progress (design/config done; runtime deferred)

The data layer is retargeted from the twin ArangoDB (graph/document) + Virtuoso
(RDF/semantic) design to a single
https://github.com/hyperpolymath/verisimdb[VeriSimDB] identity-consonance store.
One identity is an octad of modal witnesses; the graph + document witnesses
subsume ArangoDB and the semantic witness subsumes Virtuoso/RDF (see
link:ARCHITECTURE.adoc[ARCHITECTURE.adoc] and
link:.machine_readable/descriptiles/META.a2ml[META.a2ml] ADR-001).

=== Done

* [x] Retire ArangoDB + Virtuoso assets (recoverable in git)
* [x] Keep the eco/econ ontology as the semantic-witness seed at `databases/ontology/eco.ttl`
* [x] Single `databases.verisimdb` block in `config/oikos.yaml` (REST endpoint + octad map)
* [x] Retarget the policy-engine knowledge-graph bindings (SPARQL/Virtuoso → VCL/VeriSimDB; comments only)
* [x] Integration descriptor `.machine_readable/integrations/verisimdb.a2ml`
* [x] ADR-001 recorded in `META.a2ml`
* [x] Client design + AS stub scaffold (`docs/verisimdb-client.adoc`, `bot-integration-affine/src/VeriSimClient.affine`)

=== Remaining (deferred / gated)

* [ ] Implement the client — replace the stub externs (`VeriSimClient.affine`) with a real Zig-FFI adapter (mirror `hpm-*`) or a REST impl, per `connectors/shared/openapi/verisim-api-v1.yaml`
* [ ] Verify endpoint coverage (octad CRUD + search + drift) at wire-up time
* [ ] Persist analysis results + code-graph + build metrics into the octad witnesses
* [ ] Gated on: VeriSimDB maturity (v0.1.0) and the AffineScript transport (affinescript #421/#425 integration)

== v0.2 - Operational App

*Status:* planned

* [ ] Register or update the GitHub App from `.github/app.yml`
* [ ] Add webhook signature verification tests
* [ ] Add fixture-based pull request event tests
* [ ] Publish a staging deployment runbook
* [ ] Emit check runs and pull request comments from real analysis output

== v1.0 - Production Release

*Status:* planned

* [ ] Production deployment with health checks and rollback instructions
* [ ] Document installation and organization policy modes
* [ ] Define support and security response process
* [ ] Publish release artifacts and versioned App documentation
